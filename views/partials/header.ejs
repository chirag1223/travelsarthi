<nav id="navbar" class="fixed top-0 z-50 w-full bg-white/80 backdrop-blur-md border-b border-slate-100">
    <div class="container mx-auto flex h-16 items-center justify-between px-6">
        
        <div class="flex items-center">
            <a href="/" class="flex items-center">
                <div class="w-16 h-16 flex items-center"> 
                    <img src="/images/Logo_side.svg" alt="Logo" class="w-full h-full object-contain">
                </div>
            </a>
        </div>

        <div class="hidden md:flex gap-10">
             <a href="/" class="text-[11px] font-black uppercase tracking-[0.2em] text-slate-900 hover:text-orange-500 transition-colors">
                Home
            </a>
             <a href="/packages" class="text-[11px] font-black uppercase tracking-[0.2em] text-slate-900 hover:text-orange-500 transition-colors">
                Packages
            </a>
            <a href="/about" class="text-[11px] font-black uppercase tracking-[0.2em] text-slate-900 hover:text-orange-500 transition-colors">
                Our Story
            </a>
            <a href="/contact" class="text-[11px] font-black uppercase tracking-[0.2em] text-slate-900 hover:text-orange-500 transition-colors">
                Contact
            </a>
        </div>

        <div class="flex items-center gap-4">
            <a href="/contact" 
   class="inline-flex items-center justify-center h-9 px-6 bg-orange-500 text-white rounded-full text-[10px] font-black uppercase tracking-widest hover:bg-slate-900 transition-colors">
    Get a Quote
</a>

            <button id="mobile-menu-btn" onclick="toggleMobileMenu()"
                class="md:hidden p-1.5 text-slate-900 hover:text-orange-500 transition-colors">
                <i data-lucide="menu" class="h-5 w-5"></i>
            </button>
        </div>
    </div>
</nav>

<script>
    // No scroll logic here - it stays opaque/semi-transparent all the time
    function toggleMobileMenu() {
        const menu = document.getElementById('mobile-menu');
        if (menu) {
            menu.classList.toggle('hidden');
            menu.classList.toggle('flex');
        }
    }
    
    if (typeof lucide !== 'undefined') {
        lucide.createIcons();
    }
</script>